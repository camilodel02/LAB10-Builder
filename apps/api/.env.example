# Copiar a apps/api/.env (no commitear). Service role solo en el servidor.
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_SERVICE_ROLE_KEY=tu_service_role_key

# Opcional: aceptar X-User-Id sin JWT (scripts/tests locales). No usar desde el navegador.
# LAB10_ALLOW_X_USER_ID=1
